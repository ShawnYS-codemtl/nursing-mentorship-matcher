Fri, Mar 6
- created GitHub repository, project structure
- initialized Python virtual env: python -m venv venv
- activated the environment: source venv/bin/activate
- installed core dependencies: pip install flask sqlalchemy pandas
- record packages in requirements.txt: pip freeze > requirements.txt

Sat, Mar 7
- created SQLAlchemy table definitions in models/
- models/__init__.py imports the models
- database.py runs the script to initialize the db

Mon, Mar 9
- created the form template -> sent for feedback
- created google_forms.py 
- implemented process_mentor_form_submission:
    - need to change the names of rows

Tue, Mar 10
- completed form_processors.py for mentee
- linked forms to spreadsheet
- created google cloud project
    - enabled google sheets API
    - created service account credentials
    - generated credentials file
    - shared google sheet with service account
    - connect to spreadsheet in google_sheets.py

Need:
- populate table with test data using script
- test form_processors

Wed, Mar 11
- wrote the scripts to insert test data
- fixed mismatches between sheets columns and column mapping (whitespaces)
- reviewed processed data from form_processors for Mentor and Mentee
- python -m scripts.import_forms -> need to run scripts like this from project root

Fri, Mar 13
- import processed data into database
- created reset_db script

Sat, Mar 14
- started working on the scoring function
    - reviewed weights, hard constraints, scoring up to specialities
- test files in claude

Sun, Mar 15
- wrote function to convert preferred mentor/mentee names to ids -> easier for logic
- fixed bug in form with extra whitespace -> column mapping was not working
- started changing scoring based on testing (tweaking)
- thinking of locking in explicit matches before matching 
    -> need to write a function for that and update the weights

Tue, Mar 17
- updated folder structure with services packages
- fixed bug with resolve_preferences in import_forms.py -> db commits
- implemented explicit_matcher class to match explicit preferences 
- implemented scoring function, adjusting weights to not include explicit preferences
- implemented greedy algorithm (capacitated bipartite matching problem) 
- updated outputs of explicit matcher and algo to be the same 
- ran tests with run_matching.py script to check matches on dataset
- watched a video on min cost max flow

next: - implement min cost max flow algorithm
- generate match explanation function
- unit test each function?

Wed, Mar 18
- tested the max-flow-min-cost algorithm on test data
- modified data such as specific preferences to names, removed none as option
- tried modifying the mfmc algorithm for better results

Fri, Mar 20
- modified data -> made sure program and years matched for each person
- debugged issue with missing unmatched mentors ->
    - mutable state was being shared across algorithms (mentor_capacity_map)
    - greedy algorithm modified the state and flow algorithm was using the modified state 
    - solution: make a deepcopy of the state and use that instead in the greedy

Sat, Mar 21
- need to parse other languages (in case they name more than one)
- maybe need a way to identify if the answer to any question is Other for manual review
- editing form descriptions to be more clear

Sun, Mar 22
- Introduced DataSource abstraction for flexible data loading
- Added GoogleSheetsDataSource and CSVDataSource implementations
- Integrated source selection via CLI arguments (--source)
- Enabled importing mentor and mentee data from CSV files
- Preserved existing parsing and database insertion logic
- ex: (venv) (base) shawnyatsin@Shawns-MacBook-Air nursing-mentorship-matcher % python -m scripts.import_forms \   
  --source csv \
  --mentor_csv app/data/mentors.csv \
  --mentee_csv app/data/mentees.csv

Tue, Mar 24
- Wrote requirements document and implementation plan
- Will need to create a React project at the project root and separate the backend 
- created an app factory (routes/__init__.py) and registered two blueprints
- created /matches endpoint -> Return fully usable frontend data, not raw DB rows.
    - .order_by(Match.match_score.desc())   could add 

- created persist_matches.py -> function run_and_persist(matches)
    - takes the output of your matching algorithm → inserts Match records into the DB
    IMPROVEMENT LATER
    - instead of deleting everything -> only overwrite non-locked matches

- created /matching endpoint -> runs the matching algorithm and data persistance
    - returns success and count of matches created

- tested endpoints in Postman
- updated run.py to use the create_app from app factory (was getting 404 error)
- consider moving init_db from run.py after deploying to production (moving into a CLI command)

next
- implement /stats, /unmatched (done)

Thu, Mar 26
- implemented /stats, /unmatched, /matches/override 
- some ideas for future
    - /unmatched → minimal + maybe 1–2 key fields (e.g., program)
    - Detailed info → separate endpoints (ex: /mentors/<id>) 
    - Override UI → fetch full profiles dynamically
    - Preview endpoint: POST /matches/preview
    - locking mechanism endpoint: POST /matches/lock
    - modify your matching algorithm to respect locked/manual matches during re-runs (this is the next real complexity jump)
- next steps:
    - the import, export endpoint
    - frontend (simple, to test endpoints more easily like POST requests)
  
Sat, Mar 28
- implemented import_forms into API call
    - organized the script into separate services (import_data, resolve_preferences -> import_service, resolve)
    - importing csv files from a request form (would be from React UI)
- implemented export csv file of matches
    - improvements: could filter based on manual override
- tested endpoints in Postman
- next: frontend

Mon, Mar 30
- moved backend files to backend folder
- created a new virtual environment, imported dependencies
- create vite project -> frontend folder
- test API call -> need to run the backend and the frontend separately
- first implementation of dashboard page (minimal)
- installed tailwindcss -> use v3 (v4 is too complicated to use)

Wed, Apr 1
- defined types.ts for interface definitions
- fetch endpoints in api.ts
- hooks (calls fetch and stores state)
- extended on Matches table 
    - expandable row for detailed info
    - cleaner look
    - button to close/open table
- things to work on:
    - loading stats
    - loading unmatched mentees and available mentors
    - button functionality for POST endpoints
    - sorting 

Sat, Apr 4
- implemented stats panel
- implemented unmatched mentee & available mentor view
- need to: 
    - make endpoint for manual matching 
    - button functionality for POST endpoints (import, run-matching, export)
    - sorting
    - unmatching? 
    - add more fields in matches details
    - locking matches
    - flagging matches
    - column matching configuration


Sun, Apr 5
- implemented import button
    - needed to fetch POST endpoint with body {source: csv/googlesheets}
        - in the api.ts -> need headers: method, headers, body
        - pass the source when the function is called
    - added dropdown to choose between csv or google sheets -> still need to set these from UI
    - updated panels to use refreshKey props to fetch again after a POST abstraction
- implemented run-matching button
- implemented export button
    - res.blob() converts raw csv output into a downloadable file
    - fetches and then creates download link 

- ideas:
    - for available mentors/umatched mentee -> expandable row like matches for easy viewing
    - need to be able to import csv/google_sheets link directly into frontend

Tue, Apr 7
- implemented unmatching functionality
- connected manual matching frontend to backend override endpoint
- next: 
    - need to confirm that run matching on re-runs is only using remaining mentors and mentees
        -> implement locking matches
    - change the explicit choice score from 100 to actual score 
    - for available mentors/umatched mentee -> expandable row like matches for easy viewing
    - need to be able to import csv/google_sheets link directly into frontend
    - column matching configuration
    - sorting
    - column matching configuration

Fri, Apr 10
- implemented locking matches for re-running matches
- updated the score to be reflected even on explicit matches
- added score preview for manual matches, also added score breakdown
- next: 
    - add a panel for the manual matching for easier browsing of mentor/mentee

Fri, Apr 17
- added panel for manual matching for easier browsing
- updated scoring to continue weight calculation even if constraint is not met
- need:
    - need to be able to import csv/google_sheets link directly into frontend
    - column matching configuration
    - sorting


Thur, May 7
- implemented sorting of matches table by column, asc and desc
- updated match row description to include year and program for better understanding of match

Mon, May 11
- updated import functionality
    - upload mentor and mentee csv files from frontend
    - removed google sheets as an option for source
    - updated button display for more clear usage

Tue, May 12
- split importing into two phases
- Phase 1: CSV Preview 
    - user uploads files. Backend -> parses headers, samples first rows, attempts alias matching
    - frontend -> displays mappings, allows manual corrections
- Phase 2: Confirm import
    - frontend sends: uploaded file references, finalized mappings
    - backend: normalizes rows, validates types, imports

- services/importing/
    - aliases.py: MENTOR_ALIASES & MENTEE_ALIASES for matching fields to csv columns
    - schema.py: MENTOR_FIELDS & MENTEE_FIELDS define the canonical fields used in the matching
    - mapping.py: detect_mapping(cols, aliases) -> returns matching and unmatched for the preview

Wed, May 13
- two endpoints: 
    - 1. Preview/detect mapping: /import/preview
    - 2. Confirm import (functionality done)
-to do:
    - general testing
    - in normalization.py -> validate by type 
    - make import section into a component
    - make import section collapsable
    - improve UX of run matching
    - reset button
    - settings

Fri, May 22

reminders: 
running the app:
- backend % source venv/bin/activate
- backend % python run.py
- frontend % npm run dev

resetting db:
- python -m scripts.reset_db (runs as module from backend root)

- updated validation.py
- need to update normalization.py -> change the types of the raw csv
- figure out if that changes stuff

Sat, May 24
- created CLAUDE.md with project overview, dev commands, and architecture docs
- updated normalization.py: added YEAR_MAPPING, 8 type converter helpers (to_str, to_lower_str,
  to_option_str, to_int, to_year_int, to_max_mentees, to_list, to_normalized_list),
  FIELD_CONVERTERS dict, and updated normalize_rows() to apply converters
- simplified form_processors.py: removed YEAR_MAPPING, parse_checkbox_field, normalize_option;
  build_mentor_from_row and build_mentee_from_row now just read already-typed values from row dict
- extracted import section from ControlPanel into its own ImportPanel component
- ImportPanel is collapsible (▶/▼ toggle) matching the pattern of MatchesTable, StatsPanel, UnmatchedPanel
- ControlPanel is now leaner: delegates import logic entirely to ImportPanel
- dashboard UI polish pass across all components:
    - DashboardLayout: bg-gray-50 background, p-6 padding
    - Header: blue left-accent bar, McGill Nursing subtitle
    - Sidebar: border-r separator, active/hover nav item states
    - StatsPanel: large bold numbers with muted labels, 7-column stat card grid, SVG chevron collapse
    - MatchesTable: white card wrapper, match count badge, SVG chevron collapse
    - SortableHeader: fixed Tailwind JIT bug (dynamic w-[x%] class replaced with inline style), SVG sort arrow
    - MatchRow: px-3 py-2 cell padding, lock as amber/gray pill badge, unmatch as outlined pill button
    - MatchBreakdown: score rows with colored values, constraint/explicit badge pills, styled reasons list
    - UnmatchedPanel: removed duplicate score display, removed redundant mentee/mentor lists, ring hover affordance on cards, empty state, full-width match button, reasons separated from score fields in breakdown
- added reset database functionality:
    - backend: POST /reset-db endpoint (drop_all + create_all via SQLAlchemy, safe for live connection)
    - registered reset_bp in routes/__init__.py
    - frontend: resetDatabase() in api.ts, Reset DB button in ControlPanel with window.confirm guard

Sun, May 25
- reviewed and improved scoring algorithm (scoring.py):
    - removed dead commented-out early return lines in constraint checks
    - added potential_score to breakdown when constraints are violated, so admin can see
      what a pair would score if eligible (used in manual matching score preview)
    - replaced duplicated related_programs dict with _PROGRAM_CLUSTERS list + _programs_related()
      helper at module level — adding a new program now requires one entry in one place
    - increased specialty mismatch penalty from -5 to -10 for a more meaningful signal
- updated frontend to display constraint violations in manual matching panel (UnmatchedPanel.tsx):
    - red warning banner shows "Constraint violated — would score X/100 if eligible"
    - score badge turns red when constraints are violated
    - constraints_violated, potential_score, explicit_choice hidden from raw breakdown grid
      (now handled specially in the UI)
- added potential_score?: number to ScoreBreakdown type in types.ts
- updated stale test assertions in test_matching_manual.py to match current algorithm
- created UserGuidePage.tsx: 8-section walkthrough covering import, column mapping, running
  the algorithm, reading match scores, locking, manual matching, exporting, and resetting the DB
- added multi-page navigation: App.tsx now manages activePage state and routes between
  DashboardPage and UserGuidePage
- updated DashboardLayout to accept activePage and onNavigate props and thread them to Sidebar
- updated Sidebar with functional nav buttons (Dashboard / User Guide) and active state styling
- updated DashboardPage to accept and forward onNavigate prop

Mon, May 26
- added sort controls to manual matching picker lists in UnmatchedPanel:
    - Name / Program / Year buttons above mentee and mentor picker lists
    - clicking the active sort field toggles asc ▲ / desc ▼ direction
    - clicking a new field switches to it (resets to asc)
    - mentee picker uses blue highlight; mentor picker uses green highlight
    - independent sort state for each picker list
- improved import mapping UX:
    - duplicate system field highlighting: dropdowns turn red + ⚠ badge when two CSV columns
      map to the same system field; summary line lists all duplicated fields below the table
    - pre-flight duplicate check blocks Confirm Import and shows inline error without hitting the API
    - replaced generic alert("Import failed") with an inline red error panel that surfaces
      structured backend errors: missing required fields and invalid type errors with row numbers
- production deployment setup (Vercel + Render + Supabase):
    - frontend/src/services/api.ts: BASE_URL reads from VITE_API_URL env var, falls back to localhost
    - backend/app/database.py: reads DATABASE_URL env var; falls back to SQLite locally;
      auto-fixes postgres:// -> postgresql:// for SQLAlchemy 2.x compatibility
    - backend/run.py: binds to 0.0.0.0, reads PORT from env, debug off when FLASK_ENV=production
    - backend/app/routes/__init__.py: CORS origins read from ALLOWED_ORIGINS env var
    - backend/requirements.txt: added psycopg2-binary for PostgreSQL support
    - backend/render.yaml: new Render deployment config file

  Deployment steps:
    1. Supabase: create free project -> Project Settings -> Database -> Connection string (URI)
       - if password has special chars (@#%/:?&=+ space), percent-encode:
         python3 -c "import urllib.parse; print(urllib.parse.quote('password', safe=''))"
    2. Render: connect GitHub repo, set root dir to backend/
       - env vars: DATABASE_URL, ALLOWED_ORIGINS=https://<vercel-url>, FLASK_ENV=production
    3. Vercel: import GitHub repo, set root dir to frontend/
       - env var: VITE_API_URL=https://<render-url>
    4. Push code -> both services auto-deploy from GitHub

Thu, May 29
- implemented workspace isolation so each user's data is fully separated from others
- approach: shareable room code (e.g. mcgill-fall-2025) stored in localStorage,
  sent as X-Session-ID header on every API request
- backend models (Mentor, Mentee, Match): added session_id column, replaced global
  email UNIQUE constraint with composite UNIQUE(email, session_id)
- added app/utils/session.py: require_session_id() helper returns 401 if header is absent
- threaded session_id through the import pipeline:
    - import_service.py: deduplication now filters by (email, session_id)
    - form_processors.py: build_mentor/mentee_from_row() accepts and sets session_id
    - resolve.py: name lookups scoped to session
- threaded session_id through the matching pipeline:
    - locked_matches.py: filters locked matches by session_id
    - persist_matches.py: tags new matches, scopes delete to current session only
- updated all 8 route files to require and filter by session_id:
    - matches, matching, import, stats, unmatched, export, score_preview, reset
    - reset-db now deletes only the caller's workspace data (not the whole DB)
- added scripts/migrate_add_session_id.py:
    - PostgreSQL: ALTER TABLE statements; assigns existing rows to session 'default'
    - SQLite: ADD COLUMN with default 'default'
- added scripts/cleanup_workspaces.py for pruning stale sessions:
    - --list: show all workspaces with row counts and last activity
    - --days N: preview (dry run) workspaces inactive longer than N days
    - --session CODE: target a specific workspace
    - --confirm: required to actually delete anything
- frontend changes:
    - api.ts: added getSessionCode, setSessionCode, clearSessionCode helpers;
      sessionHeader() injects X-Session-ID into every fetch call
    - SessionGate.tsx: new full-screen landing shown when no code is stored;
      user types or generates a room code to join/create a workspace
    - App.tsx: renders SessionGate until a code is set, then shows the dashboard
    - Header.tsx: displays active workspace code as a pill with a Switch button
      that clears localStorage and reloads (returns to SessionGate)